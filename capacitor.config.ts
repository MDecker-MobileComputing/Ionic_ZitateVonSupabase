import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.eldecker.ionic.zitate',
  appName: 'Zitate von Supabase',
  webDir: 'www'
};

export default config;
